name: Probe M2 (ANDROID_KEYSTORE_PASSWORD)
on:
  pull_request:
    branches: [main, master]
permissions:
  contents: write
  actions: write
jobs:
  probe:
    runs-on: ubuntu-latest
    env:
      T: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
    steps:
      - run: echo hi
