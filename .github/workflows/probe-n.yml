name: Probe N (V2 secret)
on:
  pull_request:
    branches: [main, master]
permissions:
  contents: write
  actions: write
jobs:
  probe:
    runs-on: ubuntu-latest
    env:
      T: ${{ secrets.ANDROID_KEYSTORE_BASE64_V2 }}
    steps:
      - run: echo hi
