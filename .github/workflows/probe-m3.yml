name: Probe M3 (ANDROID_KEY_ALIAS)
on:
  pull_request:
    branches: [main, master]
permissions:
  contents: write
  actions: write
jobs:
  probe:
    runs-on: ubuntu-latest
    env:
      T: ${{ secrets.ANDROID_KEY_ALIAS }}
    steps:
      - run: echo hi
