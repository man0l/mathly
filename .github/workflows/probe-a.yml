# Diagnostic probe: exact trigger/concurrency/permissions top matter of
# android-release.yml with a trivial job. Delete after diagnosis.
name: Probe A (permissions)
on:
  pull_request:
    branches: [main, master]
concurrency:
  group: probe-a-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
  actions: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "probe a ok"
