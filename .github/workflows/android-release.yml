name: Android Release

concurrency:
  group: android-release-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      job:
        description: What to run
        required: true
        default: release
        type: choice
        options:
          - release
          - verify-credentials

permissions:
  contents: write
  actions: write

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx expo lint

  verify-credentials:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      EXPO_PUBLIC_API_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_BASE_URL }}
      EXPO_PUBLIC_REVENUECAT_KEY: ${{ secrets.EXPO_PUBLIC_REVENUECAT_KEY }}
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - run: bash scripts/check-android-secrets.sh

  android:
    needs: [quality, verify-credentials]
    if: github.event_name == 'workflow_dispatch' && inputs.job == 'release'
    runs-on: ubuntu-latest
    env:
      EXPO_PUBLIC_API_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_BASE_URL }}
      EXPO_PUBLIC_REVENUECAT_KEY: ${{ secrets.EXPO_PUBLIC_REVENUECAT_KEY }}
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
    steps:
      - uses: actions/checkout@v5

      - name: Set short SHA
        run: echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - run: npm ci

      # Ask Play Console for the real max versionCode across all tracks so the
      # build can never regress below what Play already served; falls back to
      # run_number when Play can't be queried (e.g. first-ever release).
      - name: Compute next Android version code
        env:
          ANDROID_PACKAGE_NAME: com.balkanbit.mathly
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          ANDROID_FALLBACK_VERSION_CODE: ${{ github.run_number }}
        run: echo "ANDROID_VERSION_CODE=$(node scripts/next-android-version-code.mjs)" >> "$GITHUB_ENV"

      - name: Generate native Android project
        run: npx expo prebuild --platform android --no-install

      - name: Configure release signing
        run: bash scripts/configure-android-signing.sh

      - name: Set version code
        run: bash scripts/set-android-version-code.sh

      - name: Build release AAB
        run: ./gradlew bundleRelease --no-daemon
        working-directory: android

      - name: Stage release artifacts
        run: |
          cp android/app/build/outputs/bundle/release/app-release.aab mathly-${{ env.SHA_SHORT }}.aab
          cp android/app/build/outputs/mapping/release/mapping.txt mapping-${{ env.SHA_SHORT }}.txt

      - name: Upload Play Store artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mathly-play-${{ env.SHA_SHORT }}
          path: |
            mathly-${{ env.SHA_SHORT }}.aab
            mapping-${{ env.SHA_SHORT }}.txt

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "android-${{ github.run_number }}" \
            "mathly-${{ env.SHA_SHORT }}.aab" \
            "mapping-${{ env.SHA_SHORT }}.txt" \
            --title "Android build ${{ env.SHA_SHORT }} (v${{ env.ANDROID_VERSION_CODE }})" \
            --notes "Play Store bundle (versionCode ${{ env.ANDROID_VERSION_CODE }}) + ProGuard mapping. Upload .aab to Internal testing, attach mapping.txt as deobfuscation file, and add testers before rollout." \
            --latest

      - name: Submit to Play Store internal track
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.balkanbit.mathly
          releaseFiles: mathly-${{ env.SHA_SHORT }}.aab
          track: internal
          mappingFile: mapping-${{ env.SHA_SHORT }}.txt

      - name: Prune old Android artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          KEEP_ARTIFACTS: "2"
        run: bash scripts/prune-android-artifacts.sh
