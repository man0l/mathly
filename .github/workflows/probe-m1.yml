name: Probe M1 (ANDROID_KEYSTORE_BASE64)
on:
  pull_request:
    branches: [main, master]
permissions:
  contents: write
  actions: write
jobs:
  probe:
    runs-on: ubuntu-latest
    env:
      T: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
    steps:
      - run: echo hi
